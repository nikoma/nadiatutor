<%= simple_form_for(@game) do |f| %>

  <div class="form-inputs">
    <%= f.input :questions %>
    <%= f.input :difficulty_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
